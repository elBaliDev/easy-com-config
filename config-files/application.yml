spring:

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    
  cloud:
    bus:
      enabled: true

management:
  health:
    probes:
      enabled: true 
  endpoints:
    web:
      exposure:
        include: "*" 
  endpoint:
    health:
      show-details: always
